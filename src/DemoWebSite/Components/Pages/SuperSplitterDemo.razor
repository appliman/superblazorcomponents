@page "/splitter-demo"
@inject NavigationManager NavigationManager

<PageTitle>SuperSplitter</PageTitle>

@code {
    protected override void OnInitialized()
    {
        NavigationManager.NavigateTo("splitter-demo/vertical", replace: true);
    }
}
